# DeepSeek / Volcengine Ark (OpenAI-compatible). Copy to .env and fill in.
# The API key is used ONLY by the Node server — never shipped to the browser.
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-flash
PORT=3001
